apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "catalog-server.fullname" . }}-test-connection"
  labels:
{{ include "catalog-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "catalog-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
