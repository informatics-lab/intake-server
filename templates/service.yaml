apiVersion: v1
kind: Service
metadata:
  name: {{ include "catalog-server.fullname" . }}
  labels:
    {{- include "catalog-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
  selector:
    {{- include "catalog-server.selectorLabels" . | nindent 4 }}
